package com.example.replayer.api;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Spring Boot entry point for the Replayer REST API. */
@SpringBootApplication
public class ReplayerApiApplication {

  /** Main method. */
  public static void main(String[] args) {
    SpringApplication.run(ReplayerApiApplication.class, args);
  }
}
